import { Server } from 'net-ipc';
import { Indomitable } from '../Indomitable';
import { Transportable } from '../Util';
export declare class Primary {
    readonly manager: Indomitable;
    readonly server: Server;
    constructor(manager: Indomitable);
    send(id: number, transportable: Transportable): Promise<any | void>;
    broadcast(transportable: Transportable): Promise<any | void>;
    private connect;
    private message;
}
//# sourceMappingURL=Primary.d.ts.map